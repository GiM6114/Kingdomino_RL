# Idea: if normal distrib centered on ((4,3),(5,3)) but this spot
# is not allowed, then take closest allowed in Manhattan distance
# and take gradient of the probability of THIS taken action

class PPO_Agent:
    def __init__(self, batch_size, clip):
        pass
    
    def action(self, state, kingdomino):
        pass
    
    def give_reward(self, reward, next_state, done, possible_actions):
        pass